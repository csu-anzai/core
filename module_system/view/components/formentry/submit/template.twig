<button type="submit" class="btn btn-default savechanges {{ class|e('html_attr') }} core-component-formentry-submit" name="{{ name|e('html_attr') }}" value="{{ value|e('html_attr') }}" {% if readOnly %}disabled="disabled"{% endif %} onclick="{{ onClick|raw }}">
    <span class="btn-text">{{ value|e('html_attr') }}</span>
    <span class="statusicon"></span>
</button>